server:
  port: 8083

spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/orion7u7/NTTDATA_Technical_Test.git
          search-paths: config-data
  application:
    name: config-server